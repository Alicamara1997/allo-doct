import 'package:flutter/material.dart';

class AppColors {
  // Primary & Accents
  static const Color primary = Color(0xFF1976D2); // Bleu médical professsionel
  static const Color primaryLight = Color(0xFF63A4FF);
  static const Color primaryDark = Color(0xFF004BA0);
  
  static const Color secondary = Color(0xFF00BCD4); // Teal pour les accents
  static const Color accent = Color(0xFFFF4081); // Pour les notifications/alertes importantes

  // Backgrounds & Surfaces
  static const Color background = Color(0xFFF8FAFD); // Très léger bleuté pour le fond
  static const Color surface = Colors.white;
  
  // Text Colors
  static const Color textMain = Color(0xFF1E293B);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color textLight = Colors.white;

  // Status Colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFE53935);
}
